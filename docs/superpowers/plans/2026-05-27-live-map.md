# Mapa ao Vivo — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Nova tela `/live-map` (admin + cliente) com mapa do Brasil mostrando emissoras monitoradas ao vivo (pulsando) + feed de últimas veiculações, alimentada por um endpoint dedicado scope-aware.

**Architecture:** Backend expõe `GET /v1/internal/live-map` (handler mockável + repo que faz duas queries, escopado por `auth.ClientScopeFromContext`). Frontend consome via react-query com polling de 20s e renderiza um SVG do Brasil com `d3-geo` (sem servidor de tiles), mais um feed de veiculações no modelo do relatório data/hora.

**Tech Stack:** Go (chi, pgx) no backend; React 19 + @tanstack/react-query + axios + `d3-geo` no frontend. GeoJSON dos estados (IBGE) embutido como asset.

**Spec:** [docs/superpowers/specs/2026-05-27-live-map-design.md](../specs/2026-05-27-live-map-design.md)

---

## File Structure

**Backend (cria):**
- `workers/internal/catalog/live_map.go` — `LiveMap` repo + structs `LiveStation`, `LiveDetection`, `LiveMapResult`.
- `workers/internal/api/handlers/live_map.go` — `LiveMapHandler` + interface `LiveMapRepo`.
- `workers/internal/api/handlers/live_map_test.go` — testes de scope + JSON shape (mock repo).

**Backend (modifica):**
- `workers/internal/api/router.go` — campo `LiveMap` em `Deps` + registro da rota no subgrupo A.
- `workers/cmd/api/main.go` — wiring `LiveMap: handlers.NewLiveMapHandler(catalog.NewLiveMap(pool))`.

**Frontend (cria):**
- `frontend/src/assets/br-states.json` — GeoJSON dos estados (IBGE).
- `frontend/src/components/BrazilMap.jsx` — mapa SVG isolado (recebe `stations`).
- `frontend/src/pages/LiveMapPage.jsx` — orquestra mapa + feed.
- `frontend/src/pages/LiveMapPage.css` — layout + animação de pulso.

**Frontend (modifica):**
- `frontend/package.json` — dep `d3-geo`.
- `frontend/src/api/hooks.js` — hook `useLiveMap()`.
- `frontend/src/App.jsx` — rota `/live-map`.
- `frontend/src/components/Sidebar.jsx` — item de menu + ícone.

**Docs (cria/modifica):**
- `docs/features/live-map.md` — doc da feature (header YAML).
- `CLAUDE.md` — linha no mapa de consulta.

---

## Task 1: Repo `catalog.LiveMap`

**Files:**
- Create: `workers/internal/catalog/live_map.go`

Esta task não tem teste unitário próprio (o repo bate no Postgres; o projeto
testa repos por integração/manual e a lógica de scope é validada no handler
da Task 2). É exercitada na verificação manual da Task 9.

- [ ] **Step 1: Escrever o repo completo**

Create `workers/internal/catalog/live_map.go`:

```go
package catalog

import (
	"context"
	"time"

	"github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgxpool"
)

// LiveStation é uma emissora monitorada a plotar no mapa ao vivo. Só retornamos
// emissoras com coordenada não-nula (geocoding pula internacionais/distritos).
type LiveStation struct {
	ID              uuid.UUID  `json:"id"`
	Name            string     `json:"name"`
	Band            string     `json:"band"`
	FrequencyMHz    *float64   `json:"frequency_mhz,omitempty"`
	City            *string    `json:"city,omitempty"`
	State           *string    `json:"state,omitempty"`
	Latitude        float64    `json:"latitude"`
	Longitude       float64    `json:"longitude"`
	HealthStatus    *string    `json:"health_status,omitempty"`
	LastDetectionAt *time.Time `json:"last_detection_at,omitempty"`
}

// LiveDetection é uma linha do feed "Últimas Veiculações" (modelo data/hora).
type LiveDetection struct {
	ID             uuid.UUID `json:"id"`
	StationName    string    `json:"station_name"`
	Band           string    `json:"band"`
	FrequencyMHz   *float64  `json:"frequency_mhz,omitempty"`
	City           *string   `json:"city,omitempty"`
	State          *string   `json:"state,omitempty"`
	DetectedAt     time.Time `json:"detected_at"`
	CommercialName string    `json:"commercial_name"`
	ClientName     *string   `json:"client_name,omitempty"`
}

type LiveMapResult struct {
	Stations         []LiveStation   `json:"stations"`
	RecentDetections []LiveDetection `json:"recent_detections"`
}

type LiveMap struct {
	pool *pgxpool.Pool
}

func NewLiveMap(pool *pgxpool.Pool) *LiveMap {
	return &LiveMap{pool: pool}
}

// Get retorna o payload do mapa escopado ao requester. scope == nil =
// admin/operator (vê todas as emissoras monitoradas + todas as veiculações);
// scope != nil = client_id do viewer (só emissoras das campanhas ativas dele +
// só as veiculações dele). $1 nulo na SQL alterna os dois caminhos.
func (m *LiveMap) Get(ctx context.Context, scope *uuid.UUID) (LiveMapResult, error) {
	var res LiveMapResult

	var scopeArg any
	if scope != nil {
		scopeArg = *scope
	}

	stations, err := m.queryStations(ctx, scopeArg)
	if err != nil {
		return res, err
	}
	res.Stations = stations

	dets, err := m.queryRecentDetections(ctx, scopeArg)
	if err != nil {
		return res, err
	}
	res.RecentDetections = dets
	return res, nil
}

func (m *LiveMap) queryStations(ctx context.Context, scopeArg any) ([]LiveStation, error) {
	rows, err := m.pool.Query(ctx, `
		SELECT s.id, s.name, s.band, s.frequency_mhz, s.city, s.state,
		       s.latitude, s.longitude, s.health_status,
		       (SELECT MAX(d.detected_at)
		          FROM detections d
		          LEFT JOIN campaigns c2 ON c2.id = d.campaign_id
		         WHERE d.station_id = s.id
		           AND ($1::uuid IS NULL OR c2.client_id = $1)
		           AND d.evidence_status <> 'audit_rejected'
		           AND d.ignored_at IS NULL) AS last_detection_at
		FROM stations s
		WHERE s.latitude IS NOT NULL AND s.longitude IS NOT NULL
		  AND (
		        ($1::uuid IS NULL AND s.monitoring_status = 'active')
		     OR ($1::uuid IS NOT NULL AND EXISTS (
		            SELECT 1 FROM campaigns cmp
		             WHERE cmp.client_id = $1
		               AND cmp.status = 'ativa'
		               AND s.id = ANY(cmp.target_stations)))
		      )
		ORDER BY s.name`, scopeArg)
	if err != nil {
		return nil, err
	}
	defer rows.Close()

	var out []LiveStation
	for rows.Next() {
		var st LiveStation
		if err := rows.Scan(&st.ID, &st.Name, &st.Band, &st.FrequencyMHz,
			&st.City, &st.State, &st.Latitude, &st.Longitude,
			&st.HealthStatus, &st.LastDetectionAt); err != nil {
			return nil, err
		}
		out = append(out, st)
	}
	return out, rows.Err()
}

func (m *LiveMap) queryRecentDetections(ctx context.Context, scopeArg any) ([]LiveDetection, error) {
	rows, err := m.pool.Query(ctx, `
		SELECT d.id, COALESCE(s.name, ''), COALESCE(s.band, ''),
		       s.frequency_mhz, s.city, s.state,
		       d.detected_at, COALESCE(m.title, c.title, ''), cli.name
		FROM detections d
		LEFT JOIN stations s    ON s.id = d.station_id
		LEFT JOIN commercials c ON c.id = d.commercial_id
		LEFT JOIN materials m   ON m.id = d.commercial_id
		LEFT JOIN campaigns cmp ON cmp.id = d.campaign_id
		LEFT JOIN clients cli   ON cli.id = cmp.client_id
		WHERE ($1::uuid IS NULL OR cmp.client_id = $1)
		  AND d.evidence_status <> 'audit_rejected'
		  AND d.ignored_at IS NULL
		  AND d.retracted_at IS NULL
		ORDER BY d.detected_at DESC
		LIMIT 50`, scopeArg)
	if err != nil {
		return nil, err
	}
	defer rows.Close()

	var out []LiveDetection
	for rows.Next() {
		var d LiveDetection
		if err := rows.Scan(&d.ID, &d.StationName, &d.Band, &d.FrequencyMHz,
			&d.City, &d.State, &d.DetectedAt, &d.CommercialName, &d.ClientName); err != nil {
			return nil, err
		}
		out = append(out, d)
	}
	return out, rows.Err()
}
```

- [ ] **Step 2: Compilar o pacote**

Run: `cd workers && go build ./internal/catalog/`
Expected: sem erros.

- [ ] **Step 3: Commit**

```bash
git add workers/internal/catalog/live_map.go
git commit -m "feat(live-map): repo catalog.LiveMap (stations + recent detections scoped)"
```

---

## Task 2: Handler `LiveMapHandler` (TDD)

**Files:**
- Create: `workers/internal/api/handlers/live_map.go`
- Test: `workers/internal/api/handlers/live_map_test.go`

- [ ] **Step 1: Escrever os testes que falham**

Create `workers/internal/api/handlers/live_map_test.go`:

```go
package handlers

import (
	"context"
	"encoding/json"
	"net/http"
	"net/http/httptest"
	"testing"

	"github.com/google/uuid"
	"radiocheck/internal/auth"
	"radiocheck/internal/catalog"
)

type fakeLiveMapRepo struct {
	gotScope    *uuid.UUID
	scopeCalled bool
	result      catalog.LiveMapResult
}

func (f *fakeLiveMapRepo) Get(ctx context.Context, scope *uuid.UUID) (catalog.LiveMapResult, error) {
	f.scopeCalled = true
	f.gotScope = scope
	return f.result, nil
}

func TestLiveMapHandler_Admin_ScopeNil(t *testing.T) {
	fake := &fakeLiveMapRepo{}
	h := &LiveMapHandler{Repo: fake}
	req := httptest.NewRequest("GET", "/live-map", nil)
	req = req.WithContext(auth.ContextWithClaims(req.Context(), &auth.Claims{Role: "admin"}))
	rr := httptest.NewRecorder()
	h.Get(rr, req)
	if rr.Code != http.StatusOK {
		t.Fatalf("status = %d, want 200", rr.Code)
	}
	if !fake.scopeCalled {
		t.Fatal("repo.Get não foi chamado")
	}
	if fake.gotScope != nil {
		t.Errorf("scope = %v, want nil para admin", fake.gotScope)
	}
}

func TestLiveMapHandler_Viewer_ScopeIsClientID(t *testing.T) {
	cid := uuid.New()
	fake := &fakeLiveMapRepo{}
	h := &LiveMapHandler{Repo: fake}
	req := httptest.NewRequest("GET", "/live-map", nil)
	req = req.WithContext(auth.ContextWithClaims(req.Context(),
		&auth.Claims{Role: "viewer", ClientID: &cid}))
	rr := httptest.NewRecorder()
	h.Get(rr, req)
	if fake.gotScope == nil || *fake.gotScope != cid {
		t.Errorf("scope = %v, want %v", fake.gotScope, cid)
	}
}

func TestLiveMapHandler_JSONShape(t *testing.T) {
	freq := 102.5
	fake := &fakeLiveMapRepo{result: catalog.LiveMapResult{
		Stations: []catalog.LiveStation{{
			ID: uuid.New(), Name: "40 Graus", Band: "FM",
			FrequencyMHz: &freq, Latitude: -20.8, Longitude: -49.3,
		}},
		RecentDetections: []catalog.LiveDetection{{
			ID: uuid.New(), StationName: "40 Graus", CommercialName: "PILECCO",
		}},
	}}
	h := &LiveMapHandler{Repo: fake}
	req := httptest.NewRequest("GET", "/live-map", nil)
	req = req.WithContext(auth.ContextWithClaims(req.Context(), &auth.Claims{Role: "admin"}))
	rr := httptest.NewRecorder()
	h.Get(rr, req)

	var body struct {
		Stations         []map[string]any `json:"stations"`
		RecentDetections []map[string]any `json:"recent_detections"`
	}
	if err := json.Unmarshal(rr.Body.Bytes(), &body); err != nil {
		t.Fatalf("unmarshal: %v", err)
	}
	if len(body.Stations) != 1 {
		t.Fatalf("stations = %d, want 1", len(body.Stations))
	}
	if len(body.RecentDetections) != 1 {
		t.Fatalf("recent_detections = %d, want 1", len(body.RecentDetections))
	}
}
```

- [ ] **Step 2: Rodar os testes e ver falhar**

Run: `cd workers && go test ./internal/api/handlers/ -run TestLiveMapHandler -v`
Expected: FAIL de compilação — `undefined: LiveMapHandler`.

- [ ] **Step 3: Escrever o handler**

Create `workers/internal/api/handlers/live_map.go`:

```go
package handlers

import (
	"context"
	"net/http"

	"github.com/google/uuid"
	"radiocheck/internal/auth"
	"radiocheck/internal/catalog"
)

// LiveMapRepo é a dependência mínima do handler. Mockável nos testes sem pool
// real (mesmo padrão de InsightsHandler).
type LiveMapRepo interface {
	Get(ctx context.Context, scope *uuid.UUID) (catalog.LiveMapResult, error)
}

type LiveMapHandler struct {
	Repo LiveMapRepo
}

func NewLiveMapHandler(repo LiveMapRepo) *LiveMapHandler {
	return &LiveMapHandler{Repo: repo}
}

// Get GET /live-map — admin vê tudo; viewer fica restrito ao próprio client_id
// via auth.ClientScopeFromContext (nil = sem scope = admin/operator).
func (h *LiveMapHandler) Get(w http.ResponseWriter, r *http.Request) {
	scope := auth.ClientScopeFromContext(r.Context())
	out, err := h.Repo.Get(r.Context(), scope)
	if err != nil {
		http.Error(w, "internal error", http.StatusInternalServerError)
		return
	}
	writeJSON(w, http.StatusOK, out)
}
```

- [ ] **Step 4: Rodar os testes e ver passar**

Run: `cd workers && go test ./internal/api/handlers/ -run TestLiveMapHandler -v`
Expected: PASS (3 testes).

- [ ] **Step 5: Commit**

```bash
git add workers/internal/api/handlers/live_map.go workers/internal/api/handlers/live_map_test.go
git commit -m "feat(live-map): handler GET /live-map scope-aware (admin/viewer)"
```

---

## Task 3: Wiring (Deps + router + main)

**Files:**
- Modify: `workers/internal/api/router.go:55` (campo no `Deps`) e `:178-180` (registro)
- Modify: `workers/cmd/api/main.go:340` (construção)

- [ ] **Step 1: Adicionar campo ao `Deps`**

Em `workers/internal/api/router.go`, no struct `Deps` (logo após a linha
`Insights *handlers.InsightsHandler`):

```go
	Insights              *handlers.InsightsHandler
	LiveMap               *handlers.LiveMapHandler
```

- [ ] **Step 2: Registrar a rota no subgrupo A (viewer-friendly)**

Em `workers/internal/api/router.go`, dentro do subgrupo A, logo após o bloco
`if d.Insights != nil { r.Get("/insights", d.Insights.Get) }`:

```go
				// Mapa ao vivo — admin vê todas as emissoras monitoradas + todas
				// as veiculações; viewer fica restrito ao próprio client via
				// auth.ClientScopeFromContext (scope no repo). Doc:
				// docs/features/live-map.md.
				if d.LiveMap != nil {
					r.Get("/live-map", d.LiveMap.Get)
				}
```

- [ ] **Step 3: Wire no main.go**

Em `workers/cmd/api/main.go`, na construção de `deps := api.Deps{...}`, logo após
a linha `Insights: handlers.NewInsightsHandler(catalog.NewInsights(pool)),`:

```go
		Insights:              handlers.NewInsightsHandler(catalog.NewInsights(pool)),
		LiveMap:               handlers.NewLiveMapHandler(catalog.NewLiveMap(pool)),
```

- [ ] **Step 4: Compilar tudo**

Run: `cd workers && go build ./...`
Expected: sem erros.

- [ ] **Step 5: Rodar a suíte de handlers + router**

Run: `cd workers && go test ./internal/api/...`
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add workers/internal/api/router.go workers/cmd/api/main.go
git commit -m "feat(live-map): registra GET /live-map no router (subgrupo viewer)"
```

---

## Task 4: GeoJSON asset dos estados (IBGE)

**Files:**
- Create: `frontend/src/assets/br-states.json`

- [ ] **Step 1: Baixar a malha de UFs do IBGE**

Run (PowerShell, na raiz do repo):

```powershell
$url = 'https://servicodados.ibge.gov.br/api/v3/malhas/paises/BR?formato=application/vnd.geo+json&qualidade=intermediaria&intrarregiao=UF'
Invoke-WebRequest -Uri $url -OutFile 'frontend/src/assets/br-states.json'
```

- [ ] **Step 2: Verificar o conteúdo**

Run:

```powershell
$j = Get-Content 'frontend/src/assets/br-states.json' -Raw | ConvertFrom-Json
$j.type; $j.features.Count; $j.features[0].properties
```

Expected: `type` = `FeatureCollection`; `features.Count` = `27`; `properties`
tem um campo `codarea` (código IBGE de 2 dígitos, ex: `"35"`). Se a malha vier
sem `codarea` mas com outro identificador (`codigo`/`id`), anote o nome — o
componente da Task 6 cobre `codarea`, `codigo`, `sigla`, `SIGLA` e `id`.

> Se o endpoint do IBGE estiver indisponível, usar a alternativa estável
> (GeoJSON com `properties.SIGLA`): baixar
> `https://raw.githubusercontent.com/giuliano-macedo/geodata-br-states/main/geojson/br_states.json`
> para o mesmo caminho. O `ufOf()` da Task 6 já lida com as duas estruturas.

- [ ] **Step 3: Commit**

```bash
git add frontend/src/assets/br-states.json
git commit -m "feat(live-map): malha GeoJSON dos estados (IBGE) como asset"
```

---

## Task 5: Dependência `d3-geo` + hook `useLiveMap()`

**Files:**
- Modify: `frontend/package.json`
- Modify: `frontend/src/api/hooks.js` (após `useStreamHealth`, ~linha 507)

- [ ] **Step 1: Instalar d3-geo**

Run: `cd frontend && npm install d3-geo@^3.1.1`
Expected: adiciona `"d3-geo"` em `dependencies` no `package.json` e atualiza o
lockfile, sem erros de peer-dep (d3-geo não depende de React).

- [ ] **Step 2: Adicionar o hook**

Em `frontend/src/api/hooks.js`, logo após a função `useStreamHealth` (que
termina por volta da linha 507):

```js
// Live map — admin vê todas as emissoras monitoradas + todas as veiculações;
// viewer é escopado ao próprio client no servidor. Polling de 20s; mantém o
// último payload bom enquanto refaz o fetch (evita "piscar" o mapa).
export function useLiveMap() {
  return useQuery({
    queryKey: ['live-map'],
    queryFn: () => api.get('/live-map').then(r => r.data),
    refetchInterval: 20_000,
    placeholderData: (prev) => prev,
  })
}
```

- [ ] **Step 3: Lint**

Run: `cd frontend && npm run lint`
Expected: sem novos erros em `hooks.js`.

- [ ] **Step 4: Commit**

```bash
git add frontend/package.json frontend/package-lock.json frontend/src/api/hooks.js
git commit -m "feat(live-map): dep d3-geo + hook useLiveMap (polling 20s)"
```

---

## Task 6: Componente `BrazilMap`

**Files:**
- Create: `frontend/src/components/BrazilMap.jsx`

- [ ] **Step 1: Escrever o componente**

Create `frontend/src/components/BrazilMap.jsx`:

```jsx
import { useMemo, useState } from 'react'
import { geoMercator, geoPath } from 'd3-geo'
import brStates from '../assets/br-states.json'

const WIDTH = 720
const HEIGHT = 760

// Código IBGE (2 dígitos) → sigla da UF. Espelha a tabela fixa do backend
// (workers/internal/geo/geo.go). Usado quando a malha vem só com codarea.
const CODE_TO_UF = {
  '11': 'RO', '12': 'AC', '13': 'AM', '14': 'RR', '15': 'PA', '16': 'AP',
  '17': 'TO', '21': 'MA', '22': 'PI', '23': 'CE', '24': 'RN', '25': 'PB',
  '26': 'PE', '27': 'AL', '28': 'SE', '29': 'BA', '31': 'MG', '32': 'ES',
  '33': 'RJ', '35': 'SP', '41': 'PR', '42': 'SC', '43': 'RS', '50': 'MS',
  '51': 'MT', '52': 'GO', '53': 'DF',
}

// ufOf cobre as estruturas de malha possíveis: SIGLA/sigla (GeoJSON com sigla),
// codarea/codigo/id (malha IBGE, código numérico → sigla via CODE_TO_UF).
function ufOf(feature) {
  const p = feature.properties || {}
  const sigla = p.SIGLA || p.sigla || p.UF || p.uf
  if (sigla) return String(sigla).toUpperCase()
  const code = String(p.codarea ?? p.codigo ?? feature.id ?? '').slice(0, 2)
  return CODE_TO_UF[code] || code
}

function dotClass(health) {
  if (health === 'ok') return 'live-dot live-dot--ok'
  if (health === 'degraded') return 'live-dot live-dot--degraded'
  return 'live-dot live-dot--down'
}

export default function BrazilMap({ stations = [] }) {
  const [hover, setHover] = useState(null)

  const { projection, pathGen } = useMemo(() => {
    const proj = geoMercator().fitSize([WIDTH, HEIGHT], brStates)
    return { projection: proj, pathGen: geoPath(proj) }
  }, [])

  const activeUFs = useMemo(() => {
    const set = new Set()
    for (const st of stations) if (st.state) set.add(String(st.state).toUpperCase())
    return set
  }, [stations])

  const points = useMemo(() => (
    stations
      .map((st) => {
        const xy = projection([st.longitude, st.latitude])
        return xy ? { st, x: xy[0], y: xy[1] } : null
      })
      .filter(Boolean)
  ), [stations, projection])

  return (
    <div className="brazil-map">
      <svg
        viewBox={`0 0 ${WIDTH} ${HEIGHT}`}
        className="brazil-map-svg"
        role="img"
        aria-label="Mapa do Brasil com emissoras monitoradas"
      >
        <g>
          {brStates.features.map((f, i) => {
            const uf = ufOf(f)
            const active = activeUFs.has(uf)
            return (
              <path
                key={uf || i}
                d={pathGen(f)}
                className={`br-state${active ? ' br-state--active' : ''}`}
              />
            )
          })}
        </g>
        <g>
          {points.map(({ st, x, y }) => (
            <g key={st.id} transform={`translate(${x} ${y})`}>
              {st.health_status === 'ok' && (
                <circle className="live-halo" r={6} />
              )}
              <circle
                className={dotClass(st.health_status)}
                r={4}
                onMouseEnter={() => setHover({ st, x, y })}
                onMouseLeave={() => setHover(null)}
              />
            </g>
          ))}
        </g>
      </svg>

      {hover && (
        <div className="map-tooltip" style={{ left: hover.x, top: hover.y }}>
          <strong>{hover.st.name}</strong>
          <span>{[hover.st.city, hover.st.state].filter(Boolean).join(' / ')}</span>
        </div>
      )}
    </div>
  )
}
```

- [ ] **Step 2: Build pra validar import do JSON + d3-geo**

Run: `cd frontend && npm run build`
Expected: build conclui sem erro (Vite importa o `.json` nativamente; `d3-geo`
resolve). Se quebrar por `geoMercator` undefined, confirmar versão de `d3-geo`
na Task 5.

- [ ] **Step 3: Commit**

```bash
git add frontend/src/components/BrazilMap.jsx
git commit -m "feat(live-map): componente BrazilMap (d3-geo, pontos por health)"
```

---

## Task 7: Página `LiveMapPage` + CSS

**Files:**
- Create: `frontend/src/pages/LiveMapPage.jsx`
- Create: `frontend/src/pages/LiveMapPage.css`

- [ ] **Step 1: Escrever o CSS**

Create `frontend/src/pages/LiveMapPage.css`:

```css
.live-map-page {
  display: grid;
  grid-template-columns: minmax(320px, 440px) 1fr;
  gap: 24px;
  padding: 24px;
  max-width: 1440px;
  margin: 0 auto;
}

.live-map-page h1 {
  grid-column: 1 / -1;
  font-family: 'Space Grotesk', sans-serif;
  font-weight: 700;
  letter-spacing: -0.02em;
  color: var(--c-text, #06055B);
  margin: 0 0 4px;
}

.live-panel {
  background: var(--c-surface, #fff);
  border: 1px solid var(--c-border, #e2e8f0);
  border-radius: 16px;
  padding: 16px;
}

.live-feed { display: flex; flex-direction: column; }
.live-feed-title {
  font-family: 'Fira Sans Condensed', sans-serif;
  font-size: 12px; text-transform: uppercase; letter-spacing: .04em;
  font-weight: 600; color: var(--c-text-2, #4b5563); margin-bottom: 12px;
}
.live-feed-list { display: flex; flex-direction: column; gap: 2px; overflow-y: auto; max-height: 70vh; }

.live-row {
  display: flex; flex-direction: column; gap: 2px;
  padding: 10px 8px; border-bottom: 1px solid var(--c-border, #e2e8f0);
  border-radius: 8px; transition: background .15s, transform .15s;
}
.live-row:hover { background: var(--c-surface-2, #f1f5f9); transform: translateY(-1px); }
.live-row-top { display: flex; justify-content: space-between; gap: 8px; }
.live-row-station { font-weight: 600; color: var(--c-text, #06055B); }
.live-row-time { font-variant-numeric: tabular-nums; color: var(--c-text-2, #4b5563); font-size: 13px; }
.live-row-meta { color: var(--c-text-3, #9ca3af); font-size: 12px; }
.live-row-material { color: var(--c-action, #E81E75); font-weight: 600; font-size: 13px; }

.live-empty { color: var(--c-text-3, #9ca3af); padding: 24px; text-align: center; }

.brazil-map { position: relative; width: 100%; }
.brazil-map-svg { width: 100%; height: auto; display: block; }

.br-state { fill: #f1f5f9; stroke: #e2e8f0; stroke-width: 0.75; transition: fill .3s; }
.br-state--active { fill: #fce7f3; stroke: #f9a8d4; }

.live-dot { stroke: #fff; stroke-width: 1; }
.live-dot--ok { fill: var(--c-action, #E81E75); }
.live-dot--degraded { fill: #f59e0b; }
.live-dot--down { fill: #9ca3af; }

.live-halo {
  fill: var(--c-action, #E81E75);
  transform-origin: center;
  transform-box: fill-box;
  animation: live-pulse 1.8s ease-out infinite;
}
@keyframes live-pulse {
  0%   { transform: scale(1);   opacity: .55; }
  70%  { transform: scale(3);   opacity: 0;   }
  100% { transform: scale(3);   opacity: 0;   }
}

.map-tooltip {
  position: absolute; transform: translate(-50%, -130%);
  background: var(--c-text, #06055B); color: #fff;
  padding: 6px 10px; border-radius: 8px; font-size: 12px;
  display: flex; flex-direction: column; gap: 2px; pointer-events: none;
  white-space: nowrap; box-shadow: 0 8px 24px rgba(6,5,91,.25);
}

@media (max-width: 900px) {
  .live-map-page { grid-template-columns: 1fr; }
}
```

- [ ] **Step 2: Escrever a página**

Create `frontend/src/pages/LiveMapPage.jsx`:

```jsx
import { useLiveMap } from '../api/hooks'
import BrazilMap from '../components/BrazilMap'
import './LiveMapPage.css'

function fmtTime(iso) {
  if (!iso) return ''
  const d = new Date(iso)
  return d.toLocaleString('pt-BR', {
    hour: '2-digit', minute: '2-digit', second: '2-digit',
    day: '2-digit', month: '2-digit', year: 'numeric',
  })
}

function stationLine(d) {
  const freq = d.frequency_mhz ? ` (${d.frequency_mhz})` : ''
  const band = d.band ? ` ${d.band}` : ''
  return `${d.station_name}${freq}${band}`.trim()
}

export default function LiveMapPage() {
  const { data, isLoading, isError } = useLiveMap()
  const stations = data?.stations ?? []
  const detections = data?.recent_detections ?? []

  return (
    <div className="live-map-page">
      <h1>Mapa ao Vivo</h1>

      <section className="live-panel live-feed">
        <span className="live-feed-title">Últimas Veiculações</span>
        <div className="live-feed-list">
          {isLoading && <div className="live-empty">Carregando…</div>}
          {isError && <div className="live-empty">Não foi possível carregar.</div>}
          {!isLoading && !isError && detections.length === 0 && (
            <div className="live-empty">Nenhuma veiculação recente.</div>
          )}
          {detections.map((d) => (
            <div className="live-row" key={d.id}>
              <div className="live-row-top">
                <span className="live-row-station">{stationLine(d)}</span>
                <span className="live-row-time">{fmtTime(d.detected_at)}</span>
              </div>
              <span className="live-row-meta">
                {[d.city, d.state].filter(Boolean).join(' / ')}
              </span>
              <span className="live-row-material">
                {[d.commercial_name, d.client_name].filter(Boolean).join(' · ')}
              </span>
            </div>
          ))}
        </div>
      </section>

      <section className="live-panel">
        {stations.length === 0 && !isLoading ? (
          <div className="live-empty">Nenhuma emissora monitorada no momento.</div>
        ) : (
          <BrazilMap stations={stations} />
        )}
      </section>
    </div>
  )
}
```

- [ ] **Step 3: Build**

Run: `cd frontend && npm run build`
Expected: build OK.

- [ ] **Step 4: Commit**

```bash
git add frontend/src/pages/LiveMapPage.jsx frontend/src/pages/LiveMapPage.css
git commit -m "feat(live-map): LiveMapPage (split mapa + feed de veiculações)"
```

---

## Task 8: Rota + item de menu

**Files:**
- Modify: `frontend/src/App.jsx:31` (import) e `:123` (rota)
- Modify: `frontend/src/components/Sidebar.jsx` (ícone + 2 links)

- [ ] **Step 1: Importar a página no App.jsx**

Em `frontend/src/App.jsx`, junto aos imports de páginas (após a linha
`import InsightsPage from './pages/InsightsPage'`):

```jsx
import InsightsPage from './pages/InsightsPage'
import LiveMapPage from './pages/LiveMapPage'
```

- [ ] **Step 2: Registrar a rota (sem RequireRole — admin + cliente)**

Em `frontend/src/App.jsx`, logo após a linha
`<Route path="/insights"    element={<InsightsPage />} />`:

```jsx
            <Route path="/insights"    element={<InsightsPage />} />
            <Route path="/live-map"    element={<LiveMapPage />} />
```

- [ ] **Step 3: Adicionar o ícone na Sidebar**

Em `frontend/src/components/Sidebar.jsx`, junto aos outros `Icon*`
(ex: após `IconInsights`/`IconDetections`), adicionar:

```jsx
function IconLiveMap() {
  return (
    <svg className="sidebar-icon" viewBox="0 0 16 16" fill="none" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round">
      <path d="M8 14s5-4.5 5-8A5 5 0 0 0 3 6c0 3.5 5 8 5 8z" />
      <circle cx="8" cy="6" r="1.75" />
    </svg>
  )
}
```

- [ ] **Step 4: Adicionar o link no AdminNav e no ClientNav**

Em `AdminNav` (seção "Veiculação"), após o link de "Indicadores":

```jsx
      <SidebarLink to="/insights"        icon={<IconInsights />}      onClose={onClose}>Indicadores</SidebarLink>
      <SidebarLink to="/live-map"        icon={<IconLiveMap />}       onClose={onClose}>Mapa ao Vivo</SidebarLink>
```

Em `ClientNav` (seção "Veiculação"), após o link de "Dashboard":

```jsx
      <SidebarLink to="/insights"        icon={<IconInsights />}      onClose={onClose}>Dashboard</SidebarLink>
      <SidebarLink to="/live-map"        icon={<IconLiveMap />}       onClose={onClose}>Mapa ao Vivo</SidebarLink>
```

- [ ] **Step 5: Lint + build**

Run: `cd frontend && npm run lint && npm run build`
Expected: sem erros; rota e menu compilam.

- [ ] **Step 6: Commit**

```bash
git add frontend/src/App.jsx frontend/src/components/Sidebar.jsx
git commit -m "feat(live-map): rota /live-map + item de menu (admin e cliente)"
```

---

## Task 9: Verificação manual (admin + cliente)

**Files:** nenhum (verificação).

- [ ] **Step 1: Subir backend + frontend local**

Conforme o fluxo de dev do projeto (ver `docs/operations/` / skill `/run`).
Garantir que o backend tem dados: emissoras com `latitude`/`longitude` (rodar
`backfill-geocoding` se necessário — ver `docs/features/geocoding-emissoras.md`).

- [ ] **Step 2: Logar como ADMIN e abrir `/live-map`**

Conferir:
- Mapa do Brasil renderiza com os estados.
- Emissoras monitoradas (com coordenada) aparecem como pontos; saudáveis pulsam
  em rosa, instáveis em âmbar, offline em cinza sem pulso.
- Estados com emissora monitorada ficam destacados (rosa claro).
- Feed "Últimas Veiculações" lista as veiculações mais recentes com data/hora,
  emissora, cidade/UF e material/cliente.
- Hover num ponto mostra tooltip.
- Após ~20s a lista/mapa atualizam sem recarregar a página.

- [ ] **Step 3: Logar como CLIENTE (viewer) e abrir `/live-map`**

Conferir o escopo:
- Só aparecem emissoras das campanhas ativas do cliente.
- Feed só mostra veiculações do próprio cliente.
- Se o cliente não tem campanha ativa → "Nenhuma emissora monitorada no momento"
  e/ou feed vazio, sem erro.

- [ ] **Step 4: Conferir no DevTools que `/live-map` é chamado e o payload bate**

Network → `GET /v1/internal/live-map` retorna `{ stations: [...], recent_detections: [...] }`.

---

## Task 10: Documentação

**Files:**
- Create: `docs/features/live-map.md`
- Modify: `CLAUDE.md` (mapa de consulta)

- [ ] **Step 1: Escrever a doc da feature**

Create `docs/features/live-map.md`:

```markdown
---
status: implementado
ultima-verificacao: 2026-05-27
codigo-relacionado:
  - workers/internal/catalog/live_map.go
  - workers/internal/api/handlers/live_map.go
  - workers/internal/api/router.go
  - frontend/src/pages/LiveMapPage.jsx
  - frontend/src/components/BrazilMap.jsx
  - frontend/src/api/hooks.js
---

# Mapa ao Vivo (/live-map)

Tela (admin + cliente) com mapa do Brasil mostrando as emissoras **monitoradas
ao vivo** pulsando + feed de **últimas veiculações**.

## Fonte de dados

`GET /v1/internal/live-map` (subgrupo viewer-friendly do router). Scope-aware via
`auth.ClientScopeFromContext`:

- **Admin/operator** (scope nil): todas as emissoras com `monitoring_status =
  'active'` e coordenada; últimas 50 veiculações.
- **Cliente** (viewer): emissoras das campanhas `status = 'ativa'` dele
  (`target_stations`); só as veiculações dele.

Só entram emissoras com `latitude`/`longitude` não-nulos (ver
[geocoding-emissoras.md](geocoding-emissoras.md)).

## Mapa

`frontend/src/components/BrazilMap.jsx` usa `d3-geo` (`geoMercator().fitSize`)
sobre a malha de UFs do IBGE embutida em `frontend/src/assets/br-states.json`.
Cada emissora vira um ponto projetado de `(longitude, latitude)`; a cor/pulso
segue `health_status` (ok = rosa pulsando, degraded = âmbar, offline = cinza).
Estado é destacado quando tem ≥1 emissora monitorada.

## Atualização

react-query (`useLiveMap`) com `refetchInterval` de 20s; mantém o último payload
durante o refetch (não pisca). O pulso é animação CSS contínua.
```

- [ ] **Step 2: Adicionar a linha no mapa de consulta do CLAUDE.md**

Em `CLAUDE.md`, na tabela "Mapa de consulta", adicionar uma linha (perto das
outras de páginas/features):

```markdown
| Mapa ao Vivo (`/live-map`, admin + cliente — emissoras monitoradas pulsando + feed de veiculações) | [docs/features/live-map.md](docs/features/live-map.md) |
```

- [ ] **Step 3: Commit**

```bash
git add docs/features/live-map.md CLAUDE.md
git commit -m "docs(live-map): feature doc + entrada no mapa de consulta"
```

---

## Verificação final

- [ ] `cd workers && go build ./... && go test ./internal/...` → PASS
- [ ] `cd frontend && npm run lint && npm run build` → OK
- [ ] Verificação manual (Task 9) confirmada para admin e cliente
- [ ] Considerar `superpowers:requesting-code-review` antes do merge

---

## Self-Review (autor do plano)

**Cobertura do spec:**
- §1 visão/escopo → Tasks 7, 8 (tela split, menu admin+cliente). ✓
- §2 backend endpoint scope-aware → Tasks 1, 2, 3. ✓
- §3 frontend (d3-geo, BrazilMap, feed, hook, asset) → Tasks 4, 5, 6, 7. ✓
- §4 data flow → Tasks 5–7. ✓
- §5 edge cases (vazio, sem coordenada, responsivo) → Task 7 (estados vazios + media query) e Task 1 (filtro de coordenada nula). ✓
- §6 design DESIGN.md → Task 7 (tokens, fontes, Soft UI). ✓
- §7 testes → Task 2 (handler) + Task 9 (manual; frontend não tem test runner no projeto). ✓
- §8 docs → Task 10. ✓

**Placeholders:** nenhum "TBD"/"TODO"; todo passo de código traz o código real.

**Consistência de tipos:** `LiveMapResult`/`LiveStation`/`LiveDetection` (Task 1)
batem com o uso no handler+teste (Task 2) e nos campos JSON consumidos pelo
frontend (`stations`, `recent_detections`, `health_status`, `frequency_mhz`,
`commercial_name`, `client_name`) em Tasks 6–7. `useLiveMap` (Task 5) → consumido
em Task 7. `BrazilMap` props (`stations`) idem.

**Nota de refinamento vs spec:** o spec citava `topojson-client`; o plano usa
GeoJSON direto (malha IBGE) + só `d3-geo`, eliminando a decodificação de
TopoJSON e uma dependência. Mesmo resultado visual, menos superfície.
